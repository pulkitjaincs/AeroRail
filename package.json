{
  "name": "aerorail",
  "private": true,
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter \"*\" --parallel dev",
    "setup:infra": "docker compose -f infra/docker-compose.yml up -d"
  }
}
